events {}

http {
    upstream my_app_cluster {
        server web_app:5000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://my_app_cluster;
        }
        
        location /stub_status {
            stub_status;
        }
    }
}